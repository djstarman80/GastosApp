name: gastos_app
description: "App de gestión de gastos personales"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # State Management
  flutter_riverpod: ^2.6.1
  
  # Database
  sqflite: ^2.4.2
  sqflite_common_ffi: ^2.3.4+4
  path_provider: ^2.1.5
  path: ^1.9.1
  
  # Dependency Injection
  get_it: ^8.0.3
  
  # Navigation
  go_router: ^14.8.1
  
  # Local Storage
  shared_preferences: ^2.3.5
  
  # Permissions
  permission_handler: ^11.3.1
  
  # Utils
  intl: ^0.20.2
  flutter_localizations:
    sdk: flutter
  
  # File handling
  file_picker: ^8.0.0
  share_plus: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # Descomenta cuando tengas la imagen PNG
  # flutter_launcher_icons: ^0.14.3

# flutter_launcher_icons:
#   android: true
#   ios: true
#   web: true
#   image_path: "assets/icon/app_icon.png"
#   adaptive_icon_background: "#4CAF50"
#   adaptive_icon_foreground: "assets/icon/app_icon_foreground.png"

flutter:
  uses-material-design: true
